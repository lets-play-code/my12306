done 1. 订票后未刷新页面
done 2. 默认不显示车票列表，只有确定出发到达站后显示
done 3. 可以查询非全程车票 (可以买半程票)
done 4. 余票应该扣除非全程票
5. 订票页面显示车次信息

Tech:
done 1. bootRun 和测试可以同时运行。需要使用不同的port和数据库
done 2. bootRun 和测试模式使用不同的前端端口，避免相互影响
done 3. 现在执行测试时需要手工启动前端。需要改成一条命令先启动前端再执行测试
done 4. 简化 Gradle 脚本：前端端口和后端地址集中在 `.env.test` 中配置，Gradle 不再传递冗余参数
done 5. 配置完全通过环境变量文件控制，使用 `loadEnv` 加载 `.env.test` 文件
done 6. 增加 AI 运行单个测试指令说明
7. 去掉目前假设测试在docker里运行的约束，目前实际是在宿主机本机跑的。这个改动影响到：docker compose 中web-driver镜像不再使用，原有约定里固定的前后端、数据库Host名称不再需要，在本机通过Hosts文件配置镜像Host名称的配置不再需要
8. 可能在AGENTS.md基础上需要增加更多的业务背景、需求、设计规范和架构的文档
9. 关闭DAL dump结果，避免误导AI判断错误原因
